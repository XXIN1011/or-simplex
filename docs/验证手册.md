# 验证手册

> 「做完」的定义：**下面该跑的都跑绿了，且数字与期望值一栏一致。**
> 改了算法层就跑第 3 节，只改界面就跑第 4 节，两者都改就都跑。
> 顺序很重要：`test/algorithm/crosscheck-*.py` 读的题库 JSON 是生成物（已 gitignore），
> 必须先跑对应的 node 脚本生成，python 才能跑。

## 1. 一键全量（最常用）

```bash
npm run verify      # 构建 + 静态检查 + 单元测试 + 8 套算法回归 + UI 回归（约 10 秒）
npm test            # 单元测试 + 8 套算法回归（末尾给汇总表）
npm run test:unit   # 只跑单元测试（7 文件 / 76 条，约 0.3 秒）
npm run check       # 静态检查：注册一致性 + 历史模块残留
```

`npm run verify` 里 UI 回归带 `--quiet`：**只在屏幕上一行**（`合计 133 通过 / 0 失败`），
明细落在 `preview/logs/verify-ui-*.log`。要读逐条明细就不带 `--quiet` 直接跑脚本。

期望值：

| 套件 | 期望输出 |
|---|---|
| 单元测试 | `# tests 76 / # pass 76 / # fail 0` |
| test-simplex（题库生成） | `随机题库已生成: random-bank.json (2000 题)` |
| edge-test | `合计: 9 通过 / 0 失败` |
| dual-test | `对偶题库已生成: dual-bank.json (400 题)` |
| sens-test | `最终结论: 全部通过 ✓（区间不多不少）` |
| scenario-test | `建表一致性自检: 与 simplex-core 完全一致 ✓` |
| param-test | `结论: PASS` |
| ip-test | `结论: PASS`（种子固定后约 3 秒） |
| assignment-test | `结论: PASS`（1200 题逐步过程自检 + 独立口径对拍，约 0.2 秒） |
| verify-ui | `合计: 133 通过 / 0 失败` |
| check-registry | `✓ 注册一致性：全部通过` |
| check-no-dp | `✅ DP 零残留：… 命中 0 处` |

**随机用例现在是可复现的**：`ip-test / param-test / scenario-test` 默认用固定种子（`--seed=3`），
套件开头会打印当次种子，失败时照着那行就能原样重跑（`node test/algorithm/ip-test.js --seed=12345` 换个批次）。
（2026-09 之前它们用 `Math.random()`，偶发失败无法复现，只能连跑三五次赌运气。）

## 2. 交叉对拍（scipy 是独立第二实现，别跳过）

```bash
node test/algorithm/test-simplex.js            # 生成 random-bank.json
python test/algorithm/crosscheck.py            # 期望：不一致: 0 条（2000/2000）

node test/algorithm/dual-test.js               # 生成 dual-bank.json
python test/algorithm/crosscheck-dual.py       # 期望：强对偶 通过 400 / 失败 0

node test/algorithm/sens-test.js               # 生成 sens-bank.json
python test/algorithm/crosscheck-sens.py       # 期望：结论: PASS

node test/algorithm/scenario-test.js           # 生成 scenario-bank.json
python test/algorithm/crosscheck-scenario.py   # 期望：结论: PASS（700 个场景算例）

node test/algorithm/assignment-test.js         # 生成 assignment-bank.json
python test/algorithm/crosscheck-assign.py     # 期望：结论: PASS（1200 题，状态 + 目标值两层）

node test/algorithm/param-test.js              # 生成 param-bank.json
python test/algorithm/crosscheck-param.py      # 期望：结论: PASS（约 1100 个抽样点）
```

题库都生成在 `test/algorithm/` 下（和生成它的脚本同目录），python 侧按脚本自身位置找，
所以从仓库根跑就行，不用先 `cd`。
CI（`.github/workflows/verify.yml` 的 `crosscheck` job）会跑这一整套，
但标成了参考性（`continue-on-error`）：HiGHS 的 presolve 在不同 scipy 版本上对无界/不可行的判定偶有差异
（见第 5 节），线上红了先看日志再判断。**本地改动算法层时仍然是必跑项。**

## 3. 改了算法层（src/core/*）——两个必跑的自证

```bash
# ① 行为等价自证：把 2000 题每次迭代的每个格子哈希，改前改后 diff 必须为空
node test/algorithm/deep-snapshot.js > preview/deep-after.txt
diff preview/deep-before.txt preview/deep-after.txt     # 无输出 = 算法行为逐字节未变

# ② 残留核查（历史模块是否彻底清零；命中会列出 file:line 并退出码 1）
node tools/check-no-dp.js
```

`deep-snapshot.js` 是重构、整理算法代码时**最有用的那一件工具**：它把每道题的
**每一次迭代、每一个表格格子、每一段显示文本**都哈希成指纹，所以
「迭代方向写反、边界条件给错但最优值碰巧相同」这类改动也躲不过去。
**改算法前先跑一次留底**，改完再跑一次 diff。

⚠️ 它只覆盖 `simplexSolve` 这一条路径（`scenario / integer / assignment` 没有逐字节护盾，
靠各自的算法回归与 scipy 对拍兜）。要动 `core/scenario.js` 这类模块时，
先想清楚「改动会不会让某个场景的判定顺序变了」，再对照 `scenario-test` 的建表一致性自检。

## 4. 只改了界面 / 样式

```bash
npm run build
node test/ui/verify-ui.js                        # 133 条断言（加 --quiet 只打一行）
node test/ui/audit.js "index.html"               # 触摸目标 / 对比度 / 表单标注 / 字号 / 横向溢出
node test/ui/audit.js "index.html#/sens"         # 逐个模块都查一遍
node test/ui/audit.js "index.html#/ip"
node test/ui/audit.js "index.html#/assign"
node test/ui/audit.js "index.html" dark          # 深色模式
node test/ui/graph-bounds.js index.html          # 改了 graph.js 或图解相关 CSS 才需要
bash tools/verify-visual.sh                      # ★ 动了颜色/半透明/玻璃相关一律要跑
```

> **改了任何与颜色、透明度、背景相关的东西，必须跑 `bash tools/verify-visual.sh`。**
> `audit.js` 的对比度是按「背景色逐层 alpha 合成」算的近似值 —— `getComputedStyle`
> 拿不到 `background-image`，页面的极光渐变不参与计算，对「压在玻璃上的文字」偏乐观。
> `verify-visual.sh` 直接读截图里文字实际压着的像素颜色，是权威校验。两者都要过。
>
> 两点使用注意：① 它会对每个模块量**两个滚动位置**（顶部 + 中段）—— 只量顶部会漏掉正文
> 深处的承载面；② 量测工具本身踩过三个坑（取框内众数会被中文粗体的墨色带偏、取「外圈」
> 对紧凑控件失效、**均匀的描边色会打败渐变的底色**），三个坑的处理都写在
> `tools/visual/measure-contrast.py` 的文件头注释里 —— 改那个脚本前先读。

### 4.1 只想确认「界面结构一个字没变」

```bash
node tools/dom-snapshot.js index.html preview/dom-after.txt
node tools/dom-snapshot.js --compare preview/dom-before.txt preview/dom-after.txt
```

`tools/dom-snapshot.js` 会把 6 个模块容器 + 底栏的**最终 DOM**（含 JS 生成出来的节点）
规范化后逐行存成文本，用 LCS 对齐做 diff。2026-09 把四段重复的输入区 HTML 收成
「JS 生成」时，就是靠它证明「除了 4 个挂载点 div，结构零差异」。

## 5. 手机视口目视（UI 改动必做）

```bash
node tools/screenshots/shot.js "index.html#/ip" "out.png" 390 0 "@fill-ip-classic.js"      # 手机视口截图
node tools/screenshots/shot.js "index.html#/ip" "out.png" 390 0 "@fill-ip-classic.js" dark # 深色模式
```

`shot.js` 用法：`node tools/screenshots/shot.js <页面[#hash]|url> <输出png> <宽> <是否点求解> [@填充脚本] [dark]`
注意三点：

1. 不带 hash 时会**默认跳到 `#/simplex`**，要看首页请显式写 `"index.html#/"`；
2. `@填充脚本` 相对 `shot.js` 自己所在目录解析（`tools/screenshots/`），所以写 `@fill-ip-classic.js`；
   输出 png 落在**仓库根**；
3. `--quiet` 可以加在任何位置，它会在脚本解析参数前把自己摘掉。

截图后**要用视觉能力真的看一眼**（`vision_analyze`），不要只依赖脚本断言 ——
历史上样式类问题（文字被裁、重叠、加粗标记 `**` 漏解析、下标没渲染）都是靠目视发现的。

## 6. ⚠️ 验证脚本本身也可能有 bug —— 遇到「偶发失败」怎么办

**背景（真实教训）**：`param-test.js` 曾经**约一半的运行会失败**，报
`该基在该 λ 处不可行; 从头重解 = infeasible`，且失败项永远锁定在同一道含人工变量的题上。
排查后发现**是引擎的真缺陷**（不是测试写错）：参数线性规划里，基中残留的人工变量必须恰好为 0，
而这条限制当初只夹了「当前行走方向」；同一个基会被向上、向下两次行走各记录一遍，
另一个方向记下的那份区间过宽，合并时恰好取了它，于是把「无可行解」的 λ 区间当成最优区间输出。

另一类「偶发失败」**不是我们的引擎错，而是裁判（HiGHS）错**（2026-09 抓到一例）：
场景对拍的 `add-var` 算例里，JS 判 `unbounded` 而 `scipy.optimize.linprog(method='highs')` 判 `infeasible`。
手算复核后**JS 是对的** ——

```
min z = -3x₁ - 5x₂ - 3x₃
s.t.  -4x₁ - 3x₂ + 2x₃ ≥ -12
        4x₁ + 2x₂ -  x₃ ≥ -9,   x ≥ 0
```

原点可行（可行性 LP 返回 `status 0`），且射线 `x(t) = (t, 0, 3t)` 对任意 `t > 0` 都满足两条约束、
`z = -12t → -∞`，数学上确定是**无界**。而各类判定方式实测为：

| 判定方式 | 结果 |
|---|---|
| `method='highs'`（presolve 默认开） | `status 2 infeasible` ✗ |
| `method='highs'`, `options={'presolve': False}` | `status 3 unbounded` ✓ |
| `method='highs-ds'`, presolve 关 | `status 3 unbounded` ✓ |
| `method='highs-ipm'`（同样走 presolve） | `status 2 infeasible` ✗ |
| `method='interior-point'`（旧版） | `unbounded` ✓ |

即 **HiGHS 的 presolve 会把「无界」误判成「无可行解」**，而 `scipy.optimize.linprog` 默认就开着 presolve。
所以「scipy 说 infeasible、我们说是 unbounded」这类分歧**必须双向怀疑**。

处置（已落地，别回退）：`crosscheck-scenario.py` 与 `crosscheck.py` 在两边状态不一致时
**关掉 presolve 再解一次**；若此时与 JS 一致，就记入 `presolve_artifacts` 并在末尾单独打印
（可复核），**不计失败也不隐藏分歧**。其余三个对拍脚本文件头也加了同一份警示。

**遇到偶发失败的通用顺序**（别一上来就改代码）：

1. 先手算一条可行射线 / 可行点，判断数学上到底该是哪个结论；
2. 换判定方式复核（`presolve=False`、换 `method=`、或换求解器）；
3. 两边都站得住，再回头怀疑引擎；
4. 复现：固定种子重跑（现在都能复现了，见第 1 节），必要时写一个最小复现脚本；
5. 修完补一条回归断言，并在注释里写清根因（历史缺陷都是这么进仓库的）。

一次性排查脚本（`diagnose-*.js`）在整理目录时已删除：缺陷都修完并补了回归，
再遇到同类问题照上面五步走、现写一个最小复现即可。

## 7. 现状自查（不用跑回归就能看的部分）

```bash
node tools/status.js            # 分支/HEAD/产物是否最新/规模/模块清单/单元测试条数/静态检查
node tools/status.js --remote   # 顺带查一次远程 SHA（需要 Clash 开着）
node tools/status.js --write     # 把现状卡写回 HANDOFF.md 的标记块之间
node tools/status.js --card      # 只打印可粘贴的 markdown 卡片
```
